namespace Repository.ProfessionalTranslator.Net
{
    public enum SaveStatus
    {
        Failed,
        PartialSuccess,
        Succeeded,
        Undetermined
    }

    public enum Display
    {
        Approved,
        Unapproved
    }
}
